PORT=5000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=spk_akreditasi
DB_USER=postgres
DB_PASSWORD=postgres

# Or use connection string (Railway)
# DATABASE_URL=postgresql://user:password@host:port/database
